# QA Report — Lucky Ledger (2026-04-07)

## Summary

Phase 1(Pixel Garden UI) + Phase 2(3x3 Grid + Pattern System) 전체 구현 후 종합 QA.
테스트 26/26 통과, TypeScript 타입체크 에러 0, 프로덕션 빌드 성공.

---

## Findings & Fixes

### CRITICAL
없음.

### IMPORTANT (2건 — 모두 수정 완료)

| # | 문제 | 파일 | 수정 내용 |
|---|------|------|-----------|
| 1 | **12라운드 승리 조건 미적용** — 게임이 12라운드 이후에도 무한 진행. ShopScene에서 moveToNextRound 후 roundIndex 체크 없음. | `src/scenes/ShopScene.ts` | roundIndex >= 12이면 gameOver = true 후 ResultScene(VICTORY!)으로 전환 |
| 2 | **RunScene 오버레이 한글 폰트 깨짐** — COMBO 가이드의 심볼 설명(한글)이 Press Start 2P(라틴 전용)로 렌더링되어 깨짐 | `src/scenes/RunScene.ts` | 한글 텍스트를 KR_FONT(Noto Sans KR)로 변경, 보상 공식도 동일 적용 |

### MINOR (4건 — 1건 수정, 3건 수용)

| # | 문제 | 파일 | 상태 |
|---|------|------|------|
| 1 | **Lucky Draw 업그레이드 색상 누락** — ShopScene의 UPGRADE_COLORS에 삭제된 banker-focus가 남아있고 lucky-draw 없음 | `src/scenes/ShopScene.ts` | **수정 완료** — lucky-draw 보라색 추가, banker-focus 삭제 |
| 2 | **HelpScene 모듈 레벨 currentPageIndex** — 페이지 인덱스가 모듈 스코프에 저장됨. 뒤로가기 시 0으로 리셋하므로 실사용 문제 없음 | `src/scenes/HelpScene.ts` | **수용** — 뒤로가기에서 리셋 처리됨 |
| 3 | **RNG seed 0 시 무한 0 출력** — Xorshift32 알고리즘 특성. Date.now() 기본값으로 실제 발생 불가 | `src/services/RngService.ts` | **수용** — 실사용 시 발생하지 않음 |
| 4 | **스코어 무조건 증가** — 매 스핀 roundIndex * 5 가산. 높은 라운드에서 스코어 인플레이션 | `src/services/EconomyService.ts` | **수용** — 의도된 디자인 (생존 보상) |

---

## Verified Areas (통과)

### Game Logic
- [x] 패턴 매칭 — 14종 패턴, Wild 대체, 전체 매치(14패턴 동시), 혼합 실패 모두 정상
- [x] 고정 스핀 규칙 — 라운드 내 spinsLeft 증가 경로 없음
- [x] 쉴드 — bomb 패턴 페널티 차단 (bankrupt은 의도적으로 미차단)
- [x] 보상 계산 — 긍정 심볼: (symVal+1)×(patVal+1)×multiplier, 부정 심볼: multiplier 미적용
- [x] 승리 조건 — roundIndex >= 12 시 VICTORY! (수정 후)
- [x] 패배 조건 — spinsLeft <= 0 && 목표 미달 시 RUN OVER
- [x] 라운드 전환 — 소지금 35%(최대 목표의 50% 캡), 리스크 55% 감소, 스핀 리셋

### Scene Flow
- [x] TitleScene → RunScene → ShopScene → RunScene 루프
- [x] RunScene → ResultScene (게임 오버)
- [x] ShopScene → ResultScene (12라운드 승리, 수정 후)
- [x] HelpScene — TitleScene/RunScene에서 진입 시 올바른 Scene으로 복귀
- [x] ShopScene picking 플래그 — create()에서 리셋
- [x] 다크모드 토글 — 모든 Scene + CSS 배경 연동

### Type Safety
- [x] SpinOutcome — grid(9), matches, totalDelta, multiplierDelta, riskDelta, message
- [x] GameRunState — wildBoost 포함 전체 필드
- [x] SymbolId — 6종 (coin, clover, star, wild, bomb, bankrupt)
- [x] tsc --noEmit 에러 0

### UI (Code Level)
- [x] 그리드 좌표 일관성 — gridCenterY, cellSize, cellGap 인스턴스 변수로 통일
- [x] 하이라이트 위치 — create()와 highlightMatches() 동일 좌표 사용
- [x] 오버레이 메모리 — overlayGroup.destroy(true)로 정리
- [x] 폰트 분리 — 영문 PX_FONT, 한글 KR_FONT (수정 후)

### Edge Cases
- [x] 전체 동일 심볼 (9칸) — 14개 패턴 모두 매치
- [x] multiplier 최대값 — maxMultiplier 캡 정상
- [x] riskMeter 100 — 0~100 클램핑, -5 자연 감소 정상
- [x] 데스크탑/모바일 — 해상도 자동 감지 (540x960 / 360x640)

---

## Test Results

```
 ✓ tests/theme.test.ts (4 tests)
 ✓ tests/patternMatcher.test.ts (9 tests)
 ✓ tests/economy.test.ts (8 tests)
 ✓ tests/upgrades.test.ts (4 tests)
 ✓ tests/simulation.test.ts (1 test)

 Test Files  5 passed (5)
 Tests       26 passed (26)
```

---

## Build

```
npm run build → ✓ built in 3.9s
tsc --noEmit → 0 errors
```
